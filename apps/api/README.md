rversed — API (minimal prototype)

Endpoints:
- GET /api/integrations/methods
- POST /api/integrations/plaid-link-token
- POST /api/integrations/buy-bitcoin

Run (PowerShell):
cd apps\api
npm install
npm run dev

Notes: seeded user `dev@rversed.test` is attached to each request for demo purposes.
